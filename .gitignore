# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
Desktop.ini

# Editor
.vscode/.history/
*.swp
*.swo
*~

# Shell history / local overrides (keep these local only)
.zsh_history
.bash_history
.zsh_sessions/

# Local secrets / env overrides — never commit these
.env
.env.local
*.secret
secrets/

# Compiled / cache
__pycache__/
*.pyc
.cache/
